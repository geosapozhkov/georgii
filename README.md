# Georgii Portfolio

Портфолио-сайт с локальным хранением файлов проектов.

## Особенности

- 📁 **Локальное хранение** - все файлы хранятся локально в папке `projects/`
- 🚀 **Простая структура** - добавьте папку проекта, и она появится на сайте
- 🖼️ **Галерея изображений и видео** - поддержка просмотра медиа-файлов в полноэкранном режиме
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🌐 **GitHub Pages ready** - готов к развёртыванию на GitHub Pages

## Структура проекта

```
GOSHA/
├── index.html              # Главная страница
├── project.html            # Универсальная страница проекта
├── css/
│   └── styles.css          # Стили
├── js/
│   ├── projects.json       # Список проектов
│   ├── main.js             # Логика главной страницы
│   └── project.js          # Логика страницы проекта
├── images/                 # Общие изображения
│   └── logo-gallery/      # Изображения для анимации логотипа
│       ├── files.json      # Список файлов (опционально)
│       ├── 01.jpg
│       ├── 02.jpg
│       └── ...
├── projects/               # Папка с проектами
│   └── Poool_Angry_Masseur/
│       └── images/         # Изображения проекта
│           ├── files.json   # Список файлов (опционально)
│           ├── 01.jpg
│           ├── 02.jpg
│           └── ...
└── README.md
```

## Как добавить новый проект

1. Создайте папку в `projects/` с именем проекта (например, `projects/My_New_Project/`)
2. Создайте папку `images/` внутри проекта
3. Добавьте изображения и видео в папку `images/` с нумерацией в имени:
   - **Изображения:** `01.jpg`, `02.jpg`, `03.jpg` (рекомендуется)
   - **Видео:** `01.mp4`, `02.mp4`, `03.mp4`
   - Также поддерживаются: `001.jpg`, `001.mp4`, `1.jpg`, `1.mp4`
   - Форматы: `image_01.jpg`, `video_01.mp4`
   - Или любой другой формат с числом в имени
4. **Сгенерируйте файл `files.json` автоматически:**
   ```bash
   node generate-files-list.js Poool_Angry_Masseur
   ```
   Или для другого проекта:
   ```bash
   node generate-files-list.js Project_Name
   ```
5. **Изображения и видео автоматически отсортируются по номеру!** Они будут отображаться в порядке нумерации, независимо от типа файла.
6. (Опционально) Можно создать файл `images/files.json` вручную:
```json
{
  "files": [
    "01.jpg",
    "02.jpg",
    "03.jpg"
  ]
}
```
6. Добавьте проект в `js/projects.json`:
```json
{
  "projects": [
    {
      "name": "My_New_Project",
      "title": "My New Project",
      "description": "Описание проекта"
    }
  ]
}
```

## Как это работает

1. **Главная страница** (`index.html`) - показывает портфолио и список всех проектов из `js/projects.json`
2. **Анимация логотипа** - при наведении на логотип появляются изображения из папки `images/logo-gallery/`
3. **Страница проекта** (`project.html`) - загружает изображения из папки `projects/PROJECT_NAME/images/`
4. При клике на проект открывается страница с галереей изображений
5. Все изображения загружаются из локальных файлов

## Развёртывание на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта
3. В настройках репозитория включите GitHub Pages
4. Выберите ветку `main` и папку `/ (root)`
5. Сайт будет доступен по адресу: `https://yourusername.github.io/repository-name/`

## Структура файлов проекта

Каждый проект должен иметь следующую структуру:

```
projects/
└── Project_Name/
    └── images/
        ├── files.json  (опционально - список файлов)
        ├── 01.jpg
        ├── 02.jpg
        ├── 03.jpg
        └── ...
```

**Автоматическая генерация списка файлов:** После добавления новых файлов в папку `images/`, запустите скрипт:
```bash
node generate-files-list.js Project_Name
```

Скрипт автоматически:
- Сканирует папку `projects/Project_Name/images/`
- Находит все изображения и видео
- Извлекает номера из имен файлов
- Сортирует по возрастанию
- Создает/обновляет файл `files.json`

**Важно:** После добавления новых файлов всегда запускайте скрипт для обновления `files.json`. Изображения и видео сортируются вместе по номерам. Например, если у вас есть `01.jpg`, `02.mp4`, `03.jpg`, они отобразятся именно в таком порядке.

## Анимация логотипа

При наведении курсора на логотип на главной странице появляются изображения из папки `images/logo-gallery/`.

1. Добавьте изображения в папку `images/logo-gallery/`
2. (Опционально) Создайте файл `images/logo-gallery/files.json` со списком файлов:
```json
{
  "files": [
    "01.jpg",
    "02.jpg",
    "03.jpg"
  ]
}
```

Если файл `files.json` отсутствует, система попытается найти файлы с стандартными именами (01.jpg, 02.jpg, и т.д.).

## Технологии

- HTML5
- CSS3 (Tailwind CSS)
- Vanilla JavaScript
- JSON для конфигурации

## Лицензия

Личный проект
